{
  "name": "fovea-stream",
  "version": "0.1.0",
  "description": "Incremental fixation-point emphasis for streaming, ANSI-coloured output — tracks SGR state and holds back partial words across chunk boundaries.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "fovea-core": "0.1.0"
  },
  "keywords": ["ansi", "stream", "reading", "accessibility", "terminal"],
  "engines": { "node": ">=18" }
}
